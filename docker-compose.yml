services:
  qdrant-service:
    image: qdrant/qdrant:v1.19.0-unprivileged
    ports:
      - 6333:6333
    #environment:
      #QDRANT__SERVICE__API_KEY: ${QDRANT_API_KEY}
    volumes:
      - "dbqdrant:/qdrant/storage"
volumes:
  dbqdrant: